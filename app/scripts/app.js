/*global define */
define(['routers/Router', 'jquery', 'bootstrap', 'vendor/rivets-backbone', 'backbone.localStorage'], function (Router) {
    'use strict';

    new Router();

    return '\'Allo \'Allo!';
});